/**
 * User id sent to MS-Assessment until your user module / JWT is integrated.
 * Replace with the real candidate id from AuthService or the gateway when ready.
 */
export const ASSESSMENT_PLACEHOLDER_USER_ID = 1;
